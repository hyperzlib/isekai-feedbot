import App from "./App";
import { EventScope } from "./PluginManager";

export class MessageStoreManager {
    private app: App;
    
    constructor(app: App) {
        this.app = app;
    }

    async initialize() {
        
    }
}